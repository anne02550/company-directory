{{!-- navbar start --}}
<nav class="navbar navbar-clean">
  <div class="container-fluid">
     
       <div class="navbar-header">
         <a class="navbar-brand" href="#">Company Directory</a>
         <a class="logout" href="/logout">Logout</a>
       </div>
 
   
  </div><!-- /.container-fluid -->
</nav>
{{!-- navbar end --}}

<div class="container">
  
  <div class="row" style="margin-top:20px">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      
        <fieldset>
            <div class="row">
                <div class="col-xs-6 col-sm-6 col-md-6">
                    <button type="submit"  id="add-employee-button" class="btn btn-lg btn-primary btn-block" >Add Employee</button>
                </div>
                <div class="col-xs-6 col-sm-6 col-md-6">
                    <button type="submit"  id="find-employee-button" class="btn btn-lg btn-success btn-block" >Find Employee</button>
                </div>
            </div> 
		      <hr class="colorgraph">
        </fieldset>

    {{!-- find-employee --}}
     {{>find-employee-form}}    

    {{!-- add-employee --}}
     {{>add-employee-form}} 

    {{!-- edit form --}}
     {{>edit-form}}   
 
    </div>
  </div>

{{!--card --}}
<div class="container" >

{{#unless personnel.length}}
  <h5 style="text-align: center;">Sorry, the employee that you want to find does not exist!</h5>
{{/unless}}
           {{#groupedEach 3 personnel}}
      <div class="row">
          {{#each this }}
        <div class="col-md-4 item">
          {{>employee-card this}}
        </div>
          {{/each}}      
      </div>
          {{/groupedEach}}
 
</div>
{{>delete-confirmation}}
    <hr class="colorgraph"> 

{{!-- container div end --}}
</div>